#!/bin/sh

if [ "$container" != "mkosi" ]; then
	exec mkosi-chroot "$CHROOT_SCRIPT" "$@"
fi

systemctl enable sshd.service

chown -R awwan:awwan /home/awwan/.ssh/
chown -R awwanssh:awwanssh /home/awwanssh/.ssh/
